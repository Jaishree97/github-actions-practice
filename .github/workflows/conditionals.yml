name: Conditionals Demo

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  push-event:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest

    steps:
      - name: Runs only on push events
        run: echo "Only runs on push events."